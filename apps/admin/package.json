{
  "name": "@3un/admin",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite --host --port 3001",
    "build": "vue-tsc -b && node build/index.js",
    "preview": "vite preview"
  }
}
